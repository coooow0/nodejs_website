<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/main.css">
</head>

<body class="chat-body">
    <%- include('nav.ejs') %>
    <div class="person-selector">
        <button class="button person-selector-button active-person" id="young-selector" onclick="updateMessageSender('영이')">영이</button>
        <button class="button person-selector-button" id="ming-selector" onclick="updateMessageSender('밍기')">밍기</button>
    </div>
    <div class="chat-container">
        <h2 class="chat-header">영이 chatting...</h2>

        <div class="chat-messages">
            <div class="message blue-bg">
                <div class="message-sender">영이</div>
                <div class="meaasge-text">어서오떼요🫧</div>
                <div class="message-timestamp"></div>
            </div>
            <div class="message gray-bg">
                <div class="message-sender">밍기</div>
                <div class="message-text">👻👻👻👻</div>
                <div class="message-timestamp"></div>
            </div>
        </div>
        <form class="chat-input-form">
            <input type="text" class="chat-input" required placeholder="Type here, 영이..." />
            <button type="submit" class="button send-button">작성</button>
        </form>
        <button class="button clear-chat-button">채팅 지우기</button>
    </div>
    <script src="app.js"></script>
    <script>
    function sendMessage(e) {
        e.preventDefault();

        const timestamp = new Date().toLocaleString('en-US', { hour: 'numeric', minute: 'numeric', hour12: true });
        const message = {
            sender: messageSender,
            text: chatInput.value,
            timestamp,
        };

        messages.push(message);
        localStorage.setItem('messages', JSON.stringify(messages));

        const chatMessages = document.querySelector('.chat-messages');
        chatMessages.innerHTML += `<div class="message">
                                        <div class="message-sender">${message.sender}</div>
                                        <div class="message-text">${message.text}</div>
                                        <div class="message-timestamp">${message.timestamp}</div>
                                    </div>`;

        chatInputForm.reset();
        chatMessages.scrollTop = chatMessages.scrollHeight;
    }
</script>

</body>

</html>
