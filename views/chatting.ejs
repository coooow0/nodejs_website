<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/main.css">
</head>

<body class="chat-body">
    <%- include('nav.ejs') %>
    <div class="person-selector">
        <button class="button person-selector-button active-person" id="young-selector"
            onclick="updateMessageSender('영이')">영이</button>
        <button class="button person-selector-button" id="ming-selector"
            onclick="updateMessageSender('밍기')">밍기</button>
    </div>
    <div class="chat-container">
        <h2 class="chat-header">영이 chatting...</h2>

        <div class="chat-messages">
            <% messages.forEach(message => { %>
            <div class="message <%= message.senderName === '영이' ? 'blue-bg' : 'gray-bg' %>">
                <div class="message-sender"><%= message.senderName %></div>
                <div class="message-text"><%= message.text %></div>
                <div class="message-timestamp"><%= message.timestamp %></div>
            </div>
            <% }) %>
        </div>
        <form class="chat-input-form" enctype="multipart/form-data" method="POST">
            <input type="text" name="text" class="chat-input" required placeholder="Type here, 영이..." />
            <input type="hidden" name="sender" value="영이"> <!-- 기본 보내는 사람 설정 -->
            <button type="submit" class="button send-button">작성</button>
        </form>
        <button class="button clear-chat-button">채팅 지우기</button>
    </div>
    <script src="app.js"></script>

</body>

</html>
